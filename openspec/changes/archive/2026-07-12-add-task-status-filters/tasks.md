## 1. 共享状态模型

- [x] 1.1 新建共享任务状态筛选模块，定义 `TaskStatusFilter`、互斥主状态优先级、状态匹配和根任务计数函数
- [x] 1.2 使用现有本地日期工具实现仅针对进行中任务的超期判定，并覆盖带时间与无截止日期的边界
- [x] 1.3 为正常状态、冲突历史标记、超期派生视图和子任务不计入根任务计数添加纯函数单元测试
- [x] 1.4 在简体中文和英文语言包中加入六种状态、计数、筛选无结果及“显示全部”文案

## 2. 桌面筛选控件与会话状态

- [x] 2.1 在 `uiStore` 增加默认值为 `all` 的会话级桌面状态筛选及更新动作，不写入持久化存储
- [x] 2.2 抽取桌面状态筛选控件，宽屏显示全部/进行中/已完成主入口及带计数的次级状态菜单
- [x] 2.3 为紧凑宽度实现单一过滤菜单形态，并确保选中次级状态时入口持续显示当前状态和计数
- [x] 2.4 为筛选控件添加键盘操作、可访问选中状态、图标名称和中英文渲染测试
- [x] 2.5 调整 `PageTitle` 的筛选接口以接收统一状态值和完整计数，移除页面对旧 `FilterMode` 的依赖

## 3. 桌面页面接入

- [x] 3.1 将 `TodayPage`、`DateFilterPage` 和 `MyDayPage` 的重复筛选表达式替换为共享状态分类与计数逻辑
- [x] 3.2 为 `TagPage` 接入同一状态筛选、计数和会话级选择保持行为
- [x] 3.3 确保列表、便签墙和一体式视图都只接收相同的筛选后根任务，并保留命中父任务的子任务上下文
- [x] 3.4 为基础集合非空但筛选结果为空的情况添加状态化空提示和“显示全部”操作，同时保留各页面原有数据为空提示
- [x] 3.5 在筛选变化时将批量选择与可见任务取交集，并在一体式详情任务被隐藏时清除 `selectedTaskId`

## 4. 筛选后的排序安全

- [x] 4.1 实现把筛选后可见顺序稳定合并回完整根任务顺序的工具函数，保持隐藏任务相对顺序
- [x] 4.2 为全量、部分可见、单项可见和无可见项的顺序合并场景添加单元测试
- [x] 4.3 调整 `TaskList` 与 `UnifiedLayout` 的手动拖拽持久化，使筛选子集不会用局部索引覆盖隐藏任务的 `sort_order`

## 5. 移动任务页接入

- [x] 5.1 将移动端 `open/done` 本地筛选替换为共享六状态模型，并停止在基础集合阶段排除已放弃任务
- [x] 5.2 保持移动任务页默认选择进行中，在搜索或标签约束后计算并展示六种状态计数
- [x] 5.3 扩展 `MobileChip` 状态行以支持全部、进行中、已完成、挂起、已放弃和超期，并验证窄屏触控尺寸与无文本遮挡
- [x] 5.4 让移动端状态筛选与搜索、标签和排序组合工作，并提供可恢复的状态化空结果

## 6. 回归测试与验收

- [x] 6.1 扩展今天、日期范围、我的一天和标签页面测试，覆盖共享计数、状态切换、会话保持与空结果恢复
- [x] 6.2 扩展桌面交互测试，覆盖三种视图集合一致、隐藏选择清理、隐藏详情关闭和状态更新后结果刷新
- [x] 6.3 扩展移动任务体验测试，覆盖默认进行中、只看挂起/已放弃、搜索与标签组合以及重新进入后的默认状态
- [x] 6.4 运行相关 Vitest 测试、完整前端测试、lint 和生产构建，并修复新增回归
- [x] 6.5 在桌面常规/紧凑宽度和 Android 常见窄屏上进行截图验收，确认控件、菜单、空状态和任务列表无重叠或截断
